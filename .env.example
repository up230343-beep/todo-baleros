# MongoDB - Reemplaza con tu URI de MongoDB Atlas
MONGODB_URI=mongodb+srv://<usuario>:<password>@cluster0.xxxxx.mongodb.net/rodamientos_db?retryWrites=true&w=majority

# Puerto (Render lo asigna automáticamente, no cambiar)
PORT=5000

# Node environment
NODE_ENV=production
